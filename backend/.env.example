# Backend Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit the .env file — it is gitignored.

# ── OpenRouter LLM ───────────────────────────────────────
# Get your key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Model to use — any OpenRouter model slug works.
# Recommended free model: arcee-ai/trinity-large-preview:free
OPENROUTER_MODEL=arcee-ai/trinity-large-preview:free

# ── Supabase PostgreSQL + pgvector ───────────────────────
# Use the Session Mode pooler URL (port 5432) from your Supabase dashboard:
# Project Settings > Database > Connection Pooling > Session mode
DATABASE_URL=postgresql://postgres.<project-ref>:<db-password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# ── CORS (Frontend URL) ──────────────────────────────────
# Set to your deployed frontend URL (no trailing slash)
# e.g. https://your-portfolio.vercel.app
FRONTEND_URL=http://localhost:5173

# ── Optional tuning (defaults shown) ────────────────────
# RATE_LIMIT_MAX_REQUESTS=20
# RATE_LIMIT_WINDOW_SECONDS=60
# TOP_K_RESULTS=8
# MAX_CHAT_HISTORY=10
# CHUNK_SIZE=300
# CHUNK_OVERLAP=50
# MAX_MESSAGE_LENGTH=1000
